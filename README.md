Python script that encrypts and decrypts text files using a simple encryption.

It allows the user to input a file for encryption or decryption.
Encrypt the file content and save it as a new file.
Provide functionality to decrypt the file back to its original form.
